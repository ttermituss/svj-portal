# ============================================================
# SVJ Portál — Environment template
# Zkopíruj jako .env a vyplň hodnoty
# NIKDY necommituj .env do gitu!
# ============================================================

# Šifrovací klíč pro citlivá nastavení v DB (AES-256-GCM)
# Vygeneruj: php -r "echo bin2hex(random_bytes(32));"
SETTINGS_ENCRYPT_KEY=
